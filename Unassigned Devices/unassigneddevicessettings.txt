Add=Aggiungi
Auto Mount USB Devices=Monta automaticamente Dispositivi USB
Case-sensitive names=Nomi sensibili alle maiuscole/minuscole
Change Disk UUID=Modifica UUID del Disco
Change UUID=Modifica UUID
Command times=Orari dei Comandi
Common Script=Script Comune
Common Settings=Generali
Destructive Mode=Modalità Distruttiva
Disabled=Disabilitato
Disk spin down is managed by Unraid=Lo spin down del disco è gestito da Unraid
Enable NFS export=Abilita esportazione NFS
Enable SMB Security=Abilita Sicurezza SMB
Enable Time Machine=Abilita Time Machine
Enter Disk Password=Inserisci Password Disco
Enter the Remote Server name or IP address and the results from the Remote Server Test will show in the Result text area=Inserisci il nome o l’indirizzo IP del Server Remoto e i risultati di 'Test Server Remoto' verranno visualizzati nella sezione **Risultato**
Enter Pool Mount Point=Inserisci Punto di Mount Pool
Legacy Mount Point Compatibility=Compatibilità Punto di Mount Legacy
Mount SSDs with discard option=Monta SSD con opzione 'discard'
NFS Security=Sicurezza NFS
NFS Settings=Impostazioni NFS
NFS must be enabled in Settings->NFS to enable NFS Export=NFS deve essere abilitato in 'Impostazioni → NFS' per abilitare l'Esportazione NFS
NFS Version to use when Mounting Remote Shares=Versione NFS per Condivisioni Remote
Public=Pubblico
Private=Privato
Private Hidden=Privato e Nascosto
Refresh and Update=Aggiorna e Ricarica
Remote Share Mount Wait Time=Tempo di attesa Mount Condivisione Remota
Rule=Regola
Select Disk=Seleziona Disco
See Help=Vedi Aiuto
Set Encrypted Disk Password=Imposta Password Disco Crittografato
Set Password=Imposta Password
Set Pool Mount Point=Imposta Punto di Mount Pool
Set the Pool Mount Point for this disk=Imposta il Punto di Mount Pool per questo disco
Set the disk Password=Imposta la Password del disco
SMB Settings=Impostazioni SMB
SMB Sharing=Condivisione SMB
Specify SMB Version when Mounting Remote Shares=Specifica Versione SMB quando si montano Condivisioni Remote
Spin down disks=Metti i dischi in standby
Test=Test
Testing=Test in corso
Test the Remote Server=Testa il Server Remoto
The Unassigned Devices Plus plugin must be installed to enable Destructive Mode=Il plugin Unassigned Devices Plus deve essere installato per abilitare la Modalità Distruttiva.
This is a tool to help troubleshoot Remote Servers ability to serve SMB and NFS shares=Questo è uno strumento per aiutare a risolvere i problemi di Servizio delle Condivisioni SMB e NFS da parte dei Server Remoti
Time Machine volume size limit=Limite dimensione volume Time Machine
to SMB share config=alla configurazione della Condivisione SMB
Udev and Hotplug=Udev e Hotplug
Unassigned Devices=Unassigned Devices
Unraid 6.10 or later is required for NFSv4 support=Unraid 6.10 o successivo è richiesto per il supporto NFSv4.
Upgrade ZFS Pool=Aggiorna Pool ZFS
Use only when directed for support because the messages can flood the log=Usalo solo quando richiesto dal supporto, perché i messaggi possono riempire il log.
Warning=Avviso
Windows may require a valid User to be defined even for Public shares=Windows potrebbe richiedere la definizione di un Utente valido anche per le Condivisioni Pubbliche.
Working=In esecuzione

; Help
;
:ud_settings_plus_plug:
> **NOTA:** Il plugin 'Unassigned Devices Plus' deve essere installato per montare i formati disco HFS+ ed exFAT e per abilitare la Modalità Distruttiva.
:end

:ud_settings_common_script_plug:
> Se impostato, questo script verrà eseguito prima dello script del dispositivo. L’azione si trova nella variabile d’ambiente **$ACTION**. Assicurati di fare riferimento a tutti i comandi con il loro percorso completo.
:end

:ud_settings_destructive_plug:
> Se abilitato, questo ti permetterà di cancellare e formattare le partizioni usando Unassigned Devices. Questa operazione non può essere abilitata a meno che il plugin 'Unassigned Devices Plus' non sia installato.
:end

:ud_settings_mount_usb_devices_plug:
> Quando impostato su **Sì**, monterà automaticamente un nuovo dispositivo USB e lo condividerà senza l’intervento dell’utente.
:end

:ud_settings_discard_plug:
> Monta gli SSD che supportano lo scarto con l’opzione 'discard'. Lascia questa impostazione su **No** se stai usando le Impostazioni Trim SSD per eseguire un trim periodico sugli SSD.
:end

:ud_settings_symlinks_plug:
> Imposta su **Sì** per creare **symlink** per '/mnt/remotes/' in '/mnt/disks/'.
>
> Questa opzione serve per la compatibilità con le versioni precedenti delle mappature di M.V. e Contenitori Docker al punto di mount '/mnt/disks/'.
> Ti consigliamo di modificare le tue mappature al punto di mount '/mnt/remotes/'.
>
> Lascia questa impostazione su **No** se non hai bisogno della retrocompatibilità.
:end

:ud_settings_remote_share_wait_plug:
> Questo valore indica il **tempo** in 'secondi' da attendere prima di montare le Condivisioni Remote quando l’array viene avviato per la prima volta.
>
:end

:ud_settings_specify_smb_vers_plug:
> Imposta su **No** se desideri che Unassigned Devices tenti prima di montare le Condivisioni Remote non specificando la versione SMB durante l’operazione di mount.
>
> Imposta su **Sì** se desideri che Unassigned Devices monti solo le Condivisioni remote SMB specificando la versione SMB.
>
> NOTA: alcuni server remoti richiedono che la versione SMB sia necessariamente specificata.
:end

:ud_settings_debug_level_plug:
> Un Livello di Log di Debug aggiunge logging aggiuntivo in aree specifiche di Unassigned Devices per la risoluzione dei problemi. Seleziona il Livello di Log di Debug per eseguire il debug di un’area specifica.
>
> Seleziona **Nessuno** per disattivare i Log di Debug.
>
> Seleziona **Udev e Hotplug** per il debug di 'udev', 'rilevamento dischi' e 'hotplug'.
>
> Seleziona **Aggiorna e Ricarica** per identificare le aree in cui Unassigned Devices si blocca.
>
> Seleziona **Orari dei Comandi** per definire i 'Tempi di esecuzione' dei comandi. **ATTENZIONE:** genera molti messaggi di log.
>
> **NOTA:** Non lasciare il 'Livello di Log' di debug abilitato per periodi prolungati in quanto può generare troppi messaggi di log.
:end

:ud_settings_specify_nfs_vers_plug:
> Seleziona **Auto** per montare tutte le Condivisioni Remote NFS, negoziando automaticamente la versione NFS da usare.
>
> Se l’opzione **Auto** non funziona o monta solo NFSv3, usa l’impostazione NFSv4 perché la tua versione di Unraid non può negoziare automaticamente tale protocollo.
:end

:ud_settings_smb_security_plug:
> Imposta il livello utente della Condivisione SMB:
>
> • Seleziona **Disabilitato** per disabilitare la Condivisione SMB.
> • Seleziona **Pubblico** per condividere i Dispositivi, le Condivisioni Remote SMB/NFS e i Mount ISO come Pubblici (con accesso in lettura e scrittura).
> • Seleziona **Privato** per abilitare la Sicurezza SMB sulle Condivisioni SMB. Imposta l’accesso per utente per consentire l’accesso e specificare se l’utente può scrivere o meno sulla Condivisione.
> • Seleziona **Privato e Nascosto** per abilitare la Sicurezza SMB sulle Condivisioni SMB nascoste. Imposta l’accesso per utente per consentire l’accesso e specificare se l’utente può scrivere o meno sulla Condivisione.
>
> Windows Server Signing:
>
> If you are unable to browse SMB shares with Windows 11 version 24H2 or newer, you may need to adjust settings to accommodate a new feature called Server Signing. Server Signing is enabled in Unraid, so additional steps may be required to access Public shares.
> You can <a href='https://techcommunity.microsoft.com/t5/storage-at-microsoft/accessing-a-third-party-nas-with-smb-in-windows-11-24h2-may-fail/ba-p/4154300' target='_blank' rel='noopener noreferrer'>disable it in Windows</a>, or to work with Unraid with Server Signing enabled, the easiest way is to create a user (with a password set) in Unraid with the same name as the Windows account you are using; Windows should then ask you for the credentials.
> If you are using a Microsoft account, it may be better to just create a user in Unraid with a simple username and set a password, then in Windows go to 'Control Panel → Credential Manager → Windows credentials → Add a Windows Credential' and add the correct Unraid server name and credentials.
:end

:ud_settings_case_sensitive_plug:
> Controls whether filenames are case-sensitive.
>
> The default setting of **Auto** allows clients that support case sensitive filenames (Linux CIFS and NFS) to tell the Samba server on a per-packet basis that they wish to access the file system in a case-sensitive manner (to support UNIX case sensitive semantics). No Windows system supports case-sensitive filenames so setting this option to auto is the same as setting it to No for them; however, the case of filenames passed by a Windows client will be preserved. This setting can result in reduced performance with very large directories because Samba must do a filename search and match on passed names.
>
> A setting of **Yes** means that files are created with the case that the client passes, and only accessible using this same case. This will speed very large directory access, but some Windows applications may not function properly with this setting. For example, if "MyFile" is created but a Windows app attempts to open "MYFILE" (which is permitted in Windows), it will not be found.
>
> A value of **Forced lower** is special: the case of all incoming client filenames, not just new filenames, will be set to lower-case. In other words, no matter what mixed case name is created on the Windows side, it will be stored and accessed in all lower-case. This ensures all Windows apps will properly find any file regardless of case, but case will not be preserved in folder listings.
:end

:ud_time_machine_plug:
> Set to **Yes** to enable Time Machine settings on the SMB shares.
:end

:ud_time_machine_vol_size_plug:
> This limits the reported volume size, preventing Time Machine from using the entire real disk space for backup. For example, setting this value to "1024" would limit the reported disk space to 1GB.
:end

:ud_settings_force_user_plug:
> There are times when permissions on SMB shares aren’t handled as expected. Enabling the **Force user** setting can help.
> This applies to all devices mounted with Unassigned Devices.
> The downside is that it overrides Linux group permissions. If you are unsure whether this is necessary, leave it disabled.
:end

:ud_settings_nfs_security_plug:
> Set to **Yes** to export Unassigned Devices with NFS sharing. Shares are exported Public with read and write access or Private with the rule you set. Access is controlled with permission settings on files and folders.
:end

:ud_settings_nfs_rules_plug:
> Put the rule for each IP address on a separate line and terminate the Rule with a new line.
> You cannot enter a Rule in the format IP1,IP2(...).  Unraid does not format the exports file in that format.

> The default rules for every NFS Rule are - async and no_subtree_check.

> **Note:**
>
> The Public default Rule used is '*(rw,sec=sys,insecure,anongid=100,anonuid=99,all_squash)'.
>
> The Private default Rule used is '*(rw,sec=sys,insecure,anongid=100,anonuid=99,no_root_squash)'.
:end

:ud_settings_disk_pass_plug:
> Select the disk that you want to set a Password. This is the password for this disk only and does not affect the array password/passphrase. Leave password blank to use the array password/passphrase for luks encrypted disks.
:end

:ud_settings_change_uuid_plug:
> Select the disk that you want to change the UUID. This is useful when a XFS formatted disk has been removed from the array and has the same UUID as an array disk. Drive must be unmounted to change the UUID.
>
> **Note:** Don’t change the UUID of a pooled disk.
:end

:ud_settings_zfs_upgrade_plug:
> Select the disk that you want to upgrade to the latest ZFS version. Drive must be mounted to upgrade the ZFS Pool.
:end

:ud_remote_server_test_plug:
> Remote Servers need to have SMB and/or NFS enabled.  This will check that the SMB and/or NFS ports are open.  The 'showmount -e <server>' command on Unraid must also show NFS mounts available.

> For SMB to be considered online, the Remote Server has to have the SMB port open.

> For NFS to be considered online, the Remote Server has to have the NFS port open and the 'showmount -e' command has to show NFS shares.
> The port may be open, but if 'showmount -e' doesn’t list any NFS shares, NFS will not work.

> If showmount does not return any NFS shares on a reachable remote server like a Synology, it generally means one of the following:

> No NFS Shares Exported:

> The server might not have any directories configured for NFS export. Check the NFS configuration on the Synology device and ensure that folders intended for NFS sharing are explicitly exported.

> Access Restrictions:

> NFS shares may be configured with IP-based access restrictions. Synology devices allow you to specify which IP addresses or networks can access each NFS share. If your IP address is not allowed, the NFS share will not appear in showmount output.

> NFS Service Not Enabled:

> The NFS service on the Synology might not be enabled. Verify this in the Synology DSM settings under Control Panel > File Services > NFS. Enable NFS if it is currently disabled.

> Firewall or Network Configuration:

> The Synology device may have firewall rules blocking the showmount request or the required RPC communication. Ensure that the firewall settings allow traffic on NFS-related ports (typically 2049 for NFS and 111 for RPC).

> Synology Export Options:

> The Synology NFS settings can sometimes require enabling Enable NFS at the top level and specifying Allow NFSv3 if you are using clients that expect NFSv3. Check Control Panel > File Services > NFS to ensure proper configuration.

> By ensuring that NFS is enabled, configured with the correct permissions, and exported directories are allowed for your client IP, showmount should list the available exports.
:end

; Work in progress | Latest revision: 17.11.2025
